# Development / analysis layer on top of requirements.txt (runtime).
#   .venv/bin/python -m pip install -r requirements.txt -r requirements-dev.txt
#
# pytest/httpx  - test suite and fastapi.testclient
# pandas        - figure-package CSVs and the .xlsx run outputs
# openpyxl      - read the 718 paired .xlsx files in data/processed
# matplotlib    - regenerate dashboard-owned figures
# scikit-optimize - gp_minimize (GP + expected improvement), the paper's BO
#                   backend (supplement S8.1); pulls in scikit-learn
pytest>=8.0
httpx>=0.27
pandas>=2.2
openpyxl>=3.1
matplotlib>=3.8
scikit-optimize>=0.10
